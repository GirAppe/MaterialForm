#if canImport(UIKit)

import UIKit

public enum MaterialFieldContextAction: String, CaseIterable {
    case copy
    case paste
    case cut
    case delete
    case select
    case selectAll
}
#endif
